import 'package:flutter/material.dart';

class BuyBookPage extends StatelessWidget {
  const BuyBookPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Buy Book')),
      body: Container(),
    );
  }
}
